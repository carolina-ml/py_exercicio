# percorrer uma lista em forma de pilha (for each)

bringmedisc = ['amo', 'Sempiternal', "There Is A Hell, Believe Me I've Seen It. There Is A Heaven, Let's Keep It A Secret", "That's The Spirit"]
for i in bringmedisc:
    if bringmedisc == 'Suicide Season':
        bringmedisc.pop()
        print(bringmedisc)
    else:
        print('oli sycks')
        
# percorrer uma lista em forma de fila - FIFO - First in Firt Out

queue = []

queue.append('Starboy')
queue.append('After Hours')
queue.append('Dawn FM')

print(queue.pop(0))

# implementando um for each

while True:
    try:
        print(queue.pop(0))
    except IndexError: 
        break
    for item in queue: 
        print(item)
